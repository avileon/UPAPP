# להריץ את UP עם אנשים אמיתיים

**קישור אחד. בלי התקנה. עובד גם באייפון.**

---

## איך זה מורכב

השרת רץ אצלך במחשב ומגיש שני דברים מאותה כתובת: את ה-API ואת האפליקציה
עצמה. Cloudflare פותח מנהרה ונותן כתובת ציבורית. אתה שולח את הכתובת — היא
נפתחת בדפדפן ככל אפליקציה.

**איך יודעים מי קרוב?** דרך **קוד מקום** — קוד קצר ששני אנשים מזינים, למשל
`BAR12`. מי שעל אותו קוד רואה זה את זה. יש מסך שמייצר QR של החדר שלכם, אז
בפועל סורקים ונכנסים. Bluetooth יגיע בגרסה הנייטיב — הדפדפן לא מסוגל לו,
ובכל מקרה קוד מקום מדויק יותר מניחוש של רדיו ולא שומר שום קואורדינטה.

---

## פעם אחת בלבד

1. **Node 22.5 ומעלה** — <https://nodejs.org>
2. **cloudflared** — `winget install --id Cloudflare.cloudflared`

---

## בכל פעם — שני חלונות

### חלון 1: השרת

```powershell
cd $HOME\Downloads\UPAPP-fix\server
```
```powershell
.\run.ps1
```

`run.ps1` מוריד את גרסת הווב האחרונה מ-GitHub, מייצר מפתח חתימה קבוע (בלעדיו
כל הפעלה מחדש מנתקת את כולם), ומפעיל את השרת. צריך להופיע:

```
UP server listening on http://0.0.0.0:3000
  database : up.db
  web app  : served from /
  sms      : mock (codes are returned in the response)
```

אם כתוב `not present` — הרץ `.\run.ps1 -Update`.

### חלון 2: המנהרה

```powershell
cloudflared tunnel --url http://localhost:3000
```

Cloudflare מדפיס כתובת בסגנון `https://something-random.trycloudflare.com`.
**זו האפליקציה.** פתח אותה בדפדפן — היא צריכה להיפתח על מסך הפתיחה של UP.

הכתובת משתנה בכל הפעלה של `cloudflared`. כל עוד החלון פתוח היא חיה.

---

## להזמין אנשים

1. פתח את הכתובת אצלך, הירשם (הקוד החד-פעמי מופיע על המסך — השרת עדיין
   ב-SMS מדומה), **והוסף תמונה אמיתית**
2. במסך הבית לחץ על שורת **חדר** ← בחר קוד, למשל `BAR12`
3. תקבל **QR וקישור**. שלח את הקישור, או תן לאנשים לסרוק את ה-QR מהמסך שלך

הקישור כולל גם את הכתובת וגם את קוד המקום, כך שמי שפותח אותו נמצא כבר בחדר
הנכון בלי להקליד כלום.

4. כל אחד נרשם עם **מספר טלפון משלו** ומוסיף תמונה
5. כולם לוחצים **Live** ← רואים אחד את השני ← UP ← Match ← צ'אט

> העדפות צריכות להסתדר בשני הכיוונים. אם אחד "גבר, מעניין אותי נשים" והשני
> "גבר, מעניין אותי נשים" — הם לא יראו זה את זה, וזה נכון. לבדיקה מהירה:
> **מעניין אותי כולם** אצל כולם.

---

## כשמשהו לא עובד

| מה רואים | מה זה | מה עושים |
|---|---|---|
| הקישור לא נפתח | המנהרה נסגרה | הרץ שוב את `cloudflared`, שלח את הכתובת החדשה |
| "אין חיבור לשרת" | חלון השרת נסגר | `.\run.ps1` שוב |
| Live דולק ואף אחד לא מופיע | קוד מקום שונה | ודא שכולם דרך אותו קישור/QR |
| כולם באותו חדר ועדיין ריק | ההעדפות לא מסתדרות | "מעניין אותי כולם" אצל כולם |
| התמונה לא נטענת | המנהרה נפלה באמצע | צא וחזור למסך |
| הכל התנתק אחרי הפעלה מחדש | היה חסר מפתח חתימה | `run.ps1` מייצר אותו — הבעיה לא תחזור |

---

## מה עוד לא אמיתי

- **BLE** — קוד המקום במקומו. הדפדפן לא מסוגל ל-BLE בכלל: אין בשום דפדפן API
  שמאפשר לעמוד לשדר כמכשיר, ובספארי אין Web Bluetooth בכלל. זה נכנס בגרסה
  הנייטיב.
- **SMS** — הקוד חוזר בתשובה של השרת ומוצג על המסך. מתאים לחברים, לא לזרים.
- **תמונות** — נשמרות ב-`server/uploads/` על המחשב שלך. כל חשבון מחובר יכול
  להביא כל תמונה אם הוא יודע את המפתח שלה; הצעד הבא הוא להחליט בכל בקשה אם
  *המשתמש הזה* רשאי לראות את *האדם הזה*.
- **זמן אמת** — האפליקציה מושכת עדכון כל 5 שניות. WebSocket הוא הצעד הבא.
- **הזמינות תלויה במחשב שלך.** נרדם, נסגר, נותק — האפליקציה מתה לכולם. לערב
  אחד עם חברים זה בסדר גמור; לשימוש לאורך ימים צריך להעביר את השרת לאירוח קבוע.
