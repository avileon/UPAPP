# שני טלפונים, שיחה אחת

מדריך ההפעלה. הוא לוקח בערך עשר דקות בפעם הראשונה ושתי דקות בכל פעם אחריה.

עד שה-BLE האמיתי ייכנס (אבן דרך 3), הגילוי נעשה דרך **קוד מקום**: שני הטלפונים
מקלידים את אותו קוד קצר, והשרת מציג אותם זה לזה. זה לא טריק זמני — זה פיצ'ר
שנשאר גם אחרי שהרדיו יגיע, כי ברדיו לבד בבר עמוס חצי מהאנשים פשוט לא נמצאים.

---

## פעם אחת בלבד

צריך שני דברים על המחשב:

1. **Node 22.5 ומעלה** — <https://nodejs.org>
2. **cloudflared** — `winget install --id Cloudflare.cloudflared`

---

## בכל פעם

### 1. הפעל את השרת

```powershell
cd server
node src/server.js
```

צריך להופיע:

```
UP server listening on http://0.0.0.0:3000
  database : up.db
  sms      : mock (codes are returned in the response)
```

### 2. פתח מנהרה — חלון PowerShell **שני**

```powershell
cloudflared tunnel --url http://localhost:3000
```

Cloudflare מדפיס כתובת בסגנון `https://something-random.trycloudflare.com`.
**זו הכתובת שהטלפונים צריכים.** היא משתנה בכל הפעלה.

בדיקה מהירה מהדפדפן: `https://<הכתובת>/health` צריך להחזיר `{"ok":true,...}`.

### 3. בכל טלפון

1. התקן את ה-APK מ-<https://github.com/avileon/UPAPP/releases/latest>
2. פתח את האפליקציה ← **הגדרות** ← **שרת**
3. הדבק את כתובת המנהרה
4. הקלד את אותו **קוד מקום** בשני הטלפונים — למשל `BAR12`
5. **שמור והתחבר** — צריך להופיע "מחובר"

### 4. הירשם בכל טלפון והוסף תמונה

מספרי טלפון **שונים** בשני המכשירים. הקוד מופיע על המסך עצמו (`קוד לפיתוח`),
כי השרת עדיין ב-SMS מדומה.

**הוסף תמונה אמיתית** במסך התמונות. בלי תמונה אין באמת מה לבדוק — הפיצ'ר
"תואם לתמונות" חסר משמעות, וגם ההחלטה אם ללחוץ UP היא לא ההחלטה האמיתית.
התמונות נשמרות אצלך במחשב, ב-`server/uploads/`, ומוגשות רק למי שמחובר.

חשוב: העדפות צריכות להסתדר בשני הכיוונים. אם בטלפון אחד "אני גבר, מעניין אותי
נשים" — בשני צריך "אני אישה, מעניין אותי גברים". הכי פשוט: **מעניין אותי כולם**
בשניהם.

### 5. לחצו Live בשני הטלפונים

תוך חמש שניות כל אחד רואה את השני. UP משני הצדדים ← Match ← צ'אט.

---

## כשמשהו לא עובד

| מה רואים | מה זה | מה עושים |
|---|---|---|
| "אין חיבור לשרת" | המנהרה נסגרה או שהכתובת השתנתה | הרץ שוב את `cloudflared`, הדבק כתובת חדשה |
| Live דולק אבל אף אחד לא מופיע | קוד מקום שונה, או שרק אחד Live | ודא שאותו קוד בדיוק בשני הטלפונים |
| שניהם Live, אותו קוד, ועדיין ריק | ההעדפות לא מסתדרות | שנה את שניהם ל"מעניין אותי כולם" |
| השרת הופעל מחדש | הסשנים נשמרים בזיכרון | האפליקציה מתחברת שוב לבד תוך כמה שניות |
| התמונה לא נטענת | המנהרה נפלה באמצע ההורדה | צא ותחזור למסך; התמונה תיטען שוב |

---

## מה עוד לא אמיתי

- **BLE** — קוד המקום מחליף אותו לעת עתה
- **SMS** — הקוד חוזר בתשובה של השרת ומוצג במסך
- **תמונות** — עובדות, אבל נשמרות בתיקייה על המחשב שלך. כל מי שמחובר יכול
  להביא כל תמונה לפי המפתח שלה; הצעד הבא הוא להחליט לכל בקשה אם *המשתמש הזה*
  רשאי לראות את *האדם הזה* — אותה שאלה ש-`/nearby/resolve` כבר עונה עליה
- **זמן אמת** — האפליקציה מושכת עדכון כל 5 שניות; WebSocket הוא הצעד הבא
