{
  "name": "up-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "UP backend — Milestone 2. Phone auth, profiles, presence, likes, matches, chat, safety.",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test \"test/*.test.js\""
  },
  "dependencies": {},
  "devDependencies": {}
}
